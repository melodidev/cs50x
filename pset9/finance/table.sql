CREATE TABLE IF NOT EXISTS 'transactions' ('id' INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, 'user_id' NUMERIC NOT NULL, 'symbol' TEXT NOT NULL, 'shares' NUMERIC NOT NULL, 'price' NUMERIC NOT NULL, 'time' DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL);